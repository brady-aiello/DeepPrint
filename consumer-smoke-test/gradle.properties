# The last published version, not the one this repository is building. This test
# consumes Maven Central, so pinning it to an unreleased version would only ever fail.
# CI ignores this and resolves the latest release: -PdeepPrintVersion=x.y.z
deepPrintVersion=0.5.0
